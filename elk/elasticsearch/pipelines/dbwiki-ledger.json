{
  "description": "Per-digest ledger events derived from .state/ingest_ledger.json by scripts/emit_derived.py (the ledger itself is a full-file rewrite, so the script emits one event per observed (path, at, status, run_id) change). @timestamp = the ledger entry's 'at'.",
  "processors": [
    { "date": { "field": "at", "target_field": "@timestamp", "formats": ["ISO8601"], "if": "ctx.at != null", "ignore_failure": true } },
    { "set": { "field": "event.module", "value": "dbwiki" } },
    { "set": { "field": "event.dataset", "value": "dbwiki.ledger" } }
  ],
  "on_failure": [
    { "append": { "field": "tags", "value": "dbwiki_pipeline_failure" } }
  ]
}
