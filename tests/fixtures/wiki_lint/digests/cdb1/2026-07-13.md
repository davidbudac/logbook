# Digest: cdb1 — 2026-07-13

Placeholder: digests are compactor output, never linted (lint.py excludes
`digests/`). It exists only so the *references* to it resolve. The real
rendered-markdown goldens live in `tests/fixtures/golden/<case>/digest.md`.
