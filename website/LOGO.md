# Logbook logo

The selected logo is [logo.png](../logo.png), the leaner database-and-book concept approved on 2026-09-09.

The symbol combines a generic database cylinder with an open book. The identity uses the name **Logbook** and has no Oracle Corporation branding.

Accompanying tagline:

> An evolving knowledge base built from database logs.

Keep this tagline as separate text; it is not part of the logo image.

The current asset is an AI-generated PNG, created using the built-in image generation tool. It is not a vector master.

The website uses [logo-transparent.png](../logo-transparent.png), an alpha-transparent
version of the selected logo. The original `logo.png` is retained unchanged.
Documentation in dark mode inverts the black artwork with CSS for contrast.

